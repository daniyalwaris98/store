# MongoDB
MONGODB_URI=mongodb://example:27077/ecommerce?directConnection=true&authSource=admin

# Session Secret (for cookie signing)
SESSION_SECRET=example

# SeaweedFS S3 ( or any S3 storage ) (self-hosted file storage)
SEAWEED_S3_ENDPOINT=https://s3.link.com
SEAWEED_S3_BUCKET=bucket_name
AWS_ACCESS_KEY_ID=example
AWS_SECRET_ACCESS_KEY=example

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=example@gmail.com
SMTP_PASS=abcd abcd abcd abcd

# Admin emails (comma-separated, hardcoded fallback)
ADMIN_EMAILS=example@email.com

# Admin Login
ADMIN_PASSWORD=example

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_CURRENCY=PKR
